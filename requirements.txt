pandas
numpy
# TA-Lib dependency removed as it cannot be installed in this environment.
# Indicators will be re-implemented using pandas.
dash
dash-bootstrap-components
python-telegram-bot
pyttsx3
aiohttp
aiofiles
# pywin32 is a Windows-only dependency for sound notifications.
# It should be installed manually on Windows systems: pip install pywin32
pytz
requests
Flask
